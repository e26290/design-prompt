請嚴格遵守以下步驟操作：

步驟 1 --- 首先檢查可用的設計系統資源

\- 首先檢查並了解所有可用的資源：

\- 局部變數

\- 局部文字樣式

\- 現有的 UI 元件

\- 現有的組件變體

\- 在檢查這些資源之前，請勿開始設計。

\- 在放置任何內容之前，請優先了解文件中已有的內容。

步驟 2 --- 嚴格的重用規則

\- 您必須只使用此文件中已有的按鈕、輸入框元件。

\- 您必須只使用此文件中已有的設計變數。

\- 對於排版，請忽略排版變量，僅使用文件中已有的局部文字樣式。

\- 請勿建立任何新的基礎 UI
元件，例如按鈕、輸入框、複選框、社交按鈕、連結或標誌。

\- 除非絕對必要，否則請勿分離組件。

\- 請盡量正確使用元件實例、元件屬性和變體屬性。

步驟 3 --- 缺失組件規則

\- 如果您找不到所需的組件，請停止並向我詢問缺少的特定組件。

\- 請我提供該組件的節點連結。

\-
如果您認為必須建立一個全新的元件，請在建立之前先向我提出一個簡單的「是」或「否」的問題。

\- 請勿自行建立或近似替代缺失的組件。

步驟 4 --- 待建立頁面

為加密貨幣錢包 Web
應用程式建立一個極簡的、淺色模式的、僅限桌面端的分割畫面登入頁面。

品牌名稱：

\- 公司名稱

整體風格：

\- 極簡

\- 簡潔

\- 產品 UI 風格

\- 僅限淺色模式

\- 盡可能使用現有變數來設定間距、顏色、圓角、邊框、陰影和佈局

\- 所有文字均使用本機文字樣式

佈局：

\- 桌面分割畫面佈局

\- 一麵包含登入表單

\- 另一含與加密錢包應用程式相關的抽象插圖

\- 插圖可以自行創作，但必須僅使用簡單的 Figma 圖形創建

\- 請勿將插圖建立為可重複使用組件

\- 保持插圖簡潔、幾何化，並符合加密錢包產品的視覺風格

\- 盡可能在頁面中使用自動佈局

表單內容和元件要求：

使用文件中的現有元件：

\- 帶有文字的 Logo 區域：公司名稱

\- 電子郵件欄位：

\- 使用輸入框組件

\- 使用合適的電子郵件樣式

\- 使用中等大小的樣式

\- 密碼欄位：

\- 使用輸入框組件

\- 使用合適的密碼樣式

\- 使用中等大小的樣式

\- 記住我：

\- 使用複選框組件

\- 忘記密碼：

\- 使用按鈕組件

\- 使用灰色連結變體

\- 主要登入操作：

\- 使用文件中現有的按鈕組件，並選擇最合適的主要變體

\- 社交按鈕：

\- 使用社交按鈕組件

\- 包含 Google 變體

\- 包含 Apple 變體

\- 如果尺寸可用，請使用中等尺寸變體

內容文字：

\- 標題：歡迎回來

\- 輔助文字：登入您的帳戶

預期表單結構：

\- 標誌/品牌

\- 標題

\- 輔助文字

\- 電子郵件字段

\- 密碼字段

\- 包含以下內容的行：

\- 記住我複選框

\- 忘記密碼連結按鈕

\- 主要登入按鈕

\-
如果存在合適的現有分隔符，則使用分隔符；否則，使用現有變數/樣式建立一個簡單的非元件分隔符

\- Google 社群按鈕

\- Apple 社交按鈕

執行規則：

\-
在整個設計中使用自動佈局，包括頁面部分、表單容器、表單內容群組、輸入群組、按鈕群組和輔助行

\- 保持間距和對齊方式的精細和一致

-桌面端請使用實際的卡片/表單尺寸

\- 保持最終佈局整潔，並可在畫布上直接用於演示

\- 清楚命名主框架

\- 請勿在文件中已有的設計系統之外進行不必要的樣式更改

\-
除非我已在上方明確指定，否則請讓文件中現有的組件系統指導選擇合適的變體

最終定稿前：

\- 確認所有文字均使用本機文字樣式

\- 確認顏色、間距、圓角、邊框和佈局值在適用情況下均使用現有變量

\- 確認所有互動控制項均為文件中的現有元件實例

\- 確認按鈕、輸入框和複選框均使用中等尺寸的變體

開始時，請簡單告知我：

1\. 您認為哪些變數相關

2\. 您認為哪些本機文字樣式相關

3\. 您計劃使用哪些現有組件

然後建立頁面。

如果缺少任何必需項，請停止並向我索取該組件節點的確切鏈接，或者在自行創建任何內容之前向我提出一個是非題。
